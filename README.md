# Dropbox Download Link Extractor

Set a env file contain your dropbox access token and change the folder you'd like to generate share links from in the script.

```
pip install -r requirements.txt
python main.py
```